// SS WebTech Deployment Configuration
// Optimized for automatic relative routing

// Use relative paths so it works on Localhost and Vercel automatically
window.API_URL = '/v1/api';

console.log(`🚀 SS WebTech API Bridge: Using Relative Path ${window.API_URL}`);
